export const validateNamaPelaku = ({value}) => {
    if(!value) return "* Nama Pelaku tidak boleh kosong"
}

export const Validatewaktu = ({value}) => {
    if(!value) return "* Waktu tidak boleh kosong"
}

export const ValidateTempat = ({value}) => {
    if(!value) return "* Tempat Kejadian tidak boleh kosong"
}

export const validateTanggal = ({value}) => {
    if(!value) return "* Tanggal tidak boleh kosong"
}

